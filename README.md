# FastAPI Project Setup

This project uses **uv** to manage Python dependencies.

## 1. Install uv If `uv` is not installed !



## 2. Install project dependencies

From the project folder, run:

```bash
uv sync
```

This automatically creates the virtual environment and installs all required dependencies.

## 3. Run the application

```bash
uv run fastapi dev
```

The application will usually be available at:

```text
http://127.0.0.1:8000
```






add : uv add alpaca-py

